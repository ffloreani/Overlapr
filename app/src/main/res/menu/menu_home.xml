<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="xyz.filipfloreani.overlapr.HomeActivity">

    <item
        android:id="@+id/sorting_item"
        android:title="@string/sorting"
        android:icon="@drawable/ic_action_sort"
        app:showAsAction="never"/>

    <item
        android:id="@+id/clear_history_item"
        android:title="@string/clear_history"
        android:icon="@drawable/ic_delete"
        app:showAsAction="never" />

    <item
        android:id="@+id/load_charts_item"
        android:title="@string/load_charts"
        app:showAsAction="never" />

    <item
        android:id="@+id/save_highlights_item"
        android:title="@string/save_highlights"
        app:showAsAction="never"/>
</menu>
